import requests

def get_weather(area):
    # Example weather fetching logic (replace with actual API call)
    # You can use OpenWeatherMap or any other weather service API
    return f"Sunny, 30°C in {area}"
